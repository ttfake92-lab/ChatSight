import { MessageTypeChart } from './MessageTypeChart'

describe('MessageTypeChart', () => {
  it('should be defined', () => {
    expect(MessageTypeChart).toBeDefined()
  })

  it('should be a function component', () => {
    expect(typeof MessageTypeChart).toBe('function')
  })
})
